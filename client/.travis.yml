sudo: required
services:
  - docker

before_install:
  - docker build -t radhitr/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run radhitr/react-test npm test -- --coverage

after_success:
  - docker build -t radhitr/multi-client ./client
  - docker build -t radhitr/multi-nginx ./nginx
  - docker build -t radhitr/multi-server ./server
  - docker build -t radhitr/multi-worker ./worker
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push radhitr/multi-client
  - docker push radhitr/multi-nginx
  - docker push radhitr/multi-server
  - docker push radhitr/multi-worker